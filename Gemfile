source "https://rubygems.org"

# Specify the theme
gem "jekyll-theme-chirpy"

# Specify the plugins used in _config.yml
# It's important that this list matches the plugins in your configuration.
gem "jekyll-feed"
gem "jekyll-paginate"
gem "jekyll-archives"
